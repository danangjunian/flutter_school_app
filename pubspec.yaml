name: school_super_app
description: Elegant school super app UI (Flutter + Supabase-ready)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.2.6
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  supabase_flutter: ^2.6.0
  mobile_scanner: ^6.0.2   # untuk scan QR
  qr_flutter: ^4.1.0       # (dipakai di sisi guru untuk generate QR)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
